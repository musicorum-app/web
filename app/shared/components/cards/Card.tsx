import { Box } from "@radix-ui/themes"
import { styled } from "styled-components"

const Card = styled(Box)`
  background: var(--gray-3);
  border: var(--border-weight) solid var(--gray-a5);
  border-radius: var(--radius-3);
`

export default Card